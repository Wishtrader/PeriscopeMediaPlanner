import SwiftUI
import CoreData

struct ContentView: View {
    // MARK: - PROPERTIES
    
    var body: some View {
        // MARK: - BODY
        SplashScreenView()
    }
}

// MARK: - PREVIEW
struct ContentView_Previews: PreviewProvider {
    static var previews: some View {
        ContentView()
    }
}
